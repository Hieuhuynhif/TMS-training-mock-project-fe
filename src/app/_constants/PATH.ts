enum PATH {
  BASE_URL = "https://tms-training-mock-project-production.up.railway.app/api",
  ITEMS = "/items",
  PRODUCTS = "/products",
  CARTS = "/carts",
  ORDERS = "/orders",
  LOGIN = "/auth/login",
  SIGNUP = "/auth/signup",
}

export default PATH;
