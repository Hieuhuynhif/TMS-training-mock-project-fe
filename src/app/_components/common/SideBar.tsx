import { Button, Stack } from "@mui/material";

function SideBar() {
  return (
    <Stack p={4}>
      <Button>Item</Button>
      <Button>Order</Button>
    </Stack>
  );
}

export default SideBar;
